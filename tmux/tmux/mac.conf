# macOS-specific overrides

# Use system clipboard where supported (tmux >= 3.2; harmless otherwise)
set -g set-clipboard on

# Optional: explicit pbcopy fallback for copy-mode-vi users.
# Uncomment if you want yanking to always go to macOS clipboard:
# bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"

# Optional: ensure default shell follows your login shell (usually zsh)
# set -g default-shell "$SHELL"